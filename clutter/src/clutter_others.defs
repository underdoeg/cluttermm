; This file is written by hand because we can't instantiate interfaces:

; ClutterMedia

(define-signal eos
  (of-object "ClutterMedia")
  (return-type "void")
  (when "last")
)

(define-signal error
  (of-object "ClutterMedia")
  (return-type "void")
  (when "last")
  (parameters
    '("GError*" "error")
  )
)
